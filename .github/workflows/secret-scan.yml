name: secret-scan

on:
  pull_request:

permissions: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  secret-scan:
    name: secret-scan
    permissions:
      contents: read
    uses: yaizuuuu/shared-workflows/.github/workflows/secret-scan.yml@da337b2d304e185cababd14ff615135bb645a4ed # v0.0.3
