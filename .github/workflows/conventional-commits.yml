name: conventional-commits

on:
  pull_request:

permissions: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  conventional-commits:
    name: conventional-commits
    permissions:
      contents: read
      pull-requests: read # PRのタイトルを取得するため
    uses: yaizuuuu/shared-workflows/.github/workflows/conventional-commits.yml@da337b2d304e185cababd14ff615135bb645a4ed # v0.0.3
